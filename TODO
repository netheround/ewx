1. [x] PSF driver (text rendering, fonts, etc)
2. [] mm - memory management (mmap, sbrk, etc)
3. [] port newlib or any other existing libc that work and match unix v4
4. [] GDT - ?? Maybe???


----- MINIMAL ----
1. [] Keep track of a static cursor variable, and implement a abstraced kputs function that dont require `x' and `y'

2. [] Write the initialization messages for psf1 font in the actual driver *.c file, same for gdt.c
